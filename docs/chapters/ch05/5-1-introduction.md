## 5.1 引言

本章将深入探讨 C++ 类的两个主要类别：

- **具体类型**（concrete types）：行为类似于内置类型的类，其表示是其定义的一部分。典型例子是 `vector` 和 `string`。
- **抽象类型**（abstract types）：将使用者与实现细节完全隔离开来的类。典型例子是 `Shape` 和 `Container`。

这两类类型的根本区别在于接口与实现的耦合程度。具体类型的接口紧密绑定于其表示，而抽象类型则通过虚函数将接口与实现解耦。

理解何时使用哪种类型，是设计良好 C++ 程序的关键。
