## 6.1 引言

每个类都需要一些基本操作来管理对象的生命周期和行为。这些操作包括：

- **构造**（construction）：创建对象
- **析构**（destruction）：销毁对象
- **拷贝**（copy）：从一个已有对象创建新的独立副本
- **移动**（move）：将资源从一个对象转移到另一个对象
- **赋值**（assignment）：用一个对象的值替换另一个对象的值

如果程序员没有显式定义这些操作，编译器会尝试自动生成。但自动生成的版本可能不符合需求——特别是当类管理着动态分配的资源时。理解这些操作的语义和生成规则，是编写正确 C++ 代码的基础。
