## 11.1 引言

C++ 的 I/O 系统基于*流*（streams）的概念。流是一个抽象的数据通道，连接着数据源和数据目的地。标准库提供了三种标准流：

- `std::cin`：标准输入
- `std::cout`：标准输出（带缓冲）
- `std::cerr`：标准错误（无缓冲）

此外，还有文件流（`std::fstream`）和字符串流（`std::stringstream`）。C++20 还引入了 `std::format`，提供了一种更现代、更安全的格式化方式。
