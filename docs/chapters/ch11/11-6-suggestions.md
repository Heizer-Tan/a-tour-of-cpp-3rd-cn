## 11.6 建议

1. 使用 `std::cout` 进行常规输出，`std::cerr` 进行错误输出
2. 使用 `std::format`（C++20）进行类型安全的格式化输出
3. 使用 `std::getline` 读取整行输入
4. 始终检查文件流是否成功打开
5. 利用 RAII 自动关闭文件——文件流在析构时自动关闭
6. 使用字符串流进行内存中的格式化操作
7. 对于二进制 I/O，使用 `std::ios::binary` 模式
8. 避免混合使用 C 风格 I/O（`printf`/`scanf`）和 C++ 流 I/O
