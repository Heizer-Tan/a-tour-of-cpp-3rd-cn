## 12.1 引言

容器（container）是用于存储对象集合的数据结构。C++ 标准库提供了多种容器，每种都有不同的性能特征和使用场景。

容器可以分为以下几类：

- **序列容器**（sequence containers）：按线性顺序存储元素
- **关联容器**（associative containers）：基于键来组织元素
- **无序关联容器**（unordered associative containers）：基于哈希表
- **容器适配器**（container adapters）：在现有容器上提供受限接口

选择正确的容器对于程序性能至关重要。
