## 13.1 引言

标准库算法是泛型编程的典范。它们通过迭代器与容器交互，实现了算法与数据结构的完全解耦。这意味着同一个算法可以用于 `vector`、`list`、`array`，甚至普通数组。

算法定义在 `<algorithm>` 和 `<numeric>` 头文件中。C++20 还引入了范围（ranges）版本的算法，定义在 `<ranges>` 中。

算法通常接受一对迭代器 `[first, last)`，表示要操作的元素范围。
