## 14.1 引言

C++20 引入了*范围*（ranges）库，这是对标准库算法和迭代器模型的重大升级。范围库的核心思想是：将"范围"（一个可以迭代的实体）作为一等公民，而非通过一对迭代器来表示。

范围库的优势：

- **更简洁的语法**：直接对容器调用算法，无需 `.begin()`/`.end()`
- **视图**（views）：惰性求值的范围适配器，可以高效组合
- **管道语法**：使用 `|` 运算符组合操作
- **更好的错误消息**：通过概念提供更清晰的约束检查
