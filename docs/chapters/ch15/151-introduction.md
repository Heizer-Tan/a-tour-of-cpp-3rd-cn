## 15.1 引言

指针是 C++ 中最基本也最强大的工具之一。它们允许直接操作内存，但也容易导致错误。现代 C++ 提供了多种抽象来安全地使用指针和引用：

- **智能指针**（smart pointers）：自动管理动态分配对象的生命周期
- **`std::span`**（C++20）：数组的只读或读写视图
- **`std::optional`**（C++17）：可能不存在的值
- **`std::variant`**（C++17）：类型安全的联合体

本章将介绍这些工具，帮助你在保持安全性的同时充分利用指针的能力。
